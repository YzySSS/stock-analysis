# Stock Analysis 仅把 Redis 用作可丢弃缓存，不保存业务事实。
bind 127.0.0.1 ::1
protected-mode yes
port 6379

maxmemory 192mb
maxmemory-policy allkeys-lfu

# MySQL 是唯一事实源，因此关闭 Redis 持久化。
save ""
appendonly no

timeout 0
tcp-keepalive 60
databases 2
