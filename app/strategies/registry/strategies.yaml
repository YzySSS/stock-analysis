default_strategy: null

strategies:
  - id: a_share_sentiment
    display_name: A股舆情选股
    entrypoint: app.strategies.active.thematic_strategies.AShareSentimentStrategy
    config_path: app/strategies/registry/configs/a_share_sentiment.yaml
    version: 0.4.4
    status: experimental
    mode: current
    executable: true
    capability:
      supported_instrument_types: [stock]
      required_datasets: [daily_kline, factor_input_daily, sector_opinion_daily]
      minimum_coverage: 0.95
      maximum_data_age_days: 1
      runtime_status: enabled
      backtest_status: disabled
      validation_status: unvalidated
      evidence_status: none
      evidence_note: 0.4.4完成实体消歧、历史缓存出口校验、局部方向与局部事件时效持久化；旧版样本不沿用，当前未通过正式样本外验证
    tags: [sentiment, news, moneyflow, market-index, risk-control, experimental]
    description: 贴合 A 股新闻、公告和题材驱动特征，优先使用本地舆情缓存，并支持候选池渐进精排
