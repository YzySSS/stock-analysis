# 数据补全下一阶段执行清单

更新时间：2026-04-28 16:36

## 当前目标

V1 页面版已基本完成，当前主线已经从“先把日线补起来”切换到：

> **优先提升基本面与估值覆盖率，并为筹码/资金流接口接入做好主线准备。**

当前不再以页面扩展为主，而以：

1. 基本面字段补齐
2. 估值字段补齐
3. 把新 Tushare 接口纳入 `app/` 主线
4. 让选股 / 复盘 / 详情页中的关键字段更真实

---

## 一、当前数据状态（最新确认）

### 1. 股票总量
- `stock_basic(stock)`：**5200**

### 2. 历史 K 线
- 已覆盖股票数：**5200 / 5200**
- 覆盖率：**100.0%**（股票口径）
- 总行数：**1127355**
- 最新交易日：**2026-04-27**

### 3. 基本面覆盖
- 验证批次后已覆盖股票数：**82 / 5200**
- 覆盖率：**1.58%**
- 最近基本面验证成功任务：`fundamental_sync_20260428_163319`
- 该批次结果：`scanned=20 / updated=20 / failed=0 / throttled=0`

### 4. 估值覆盖
- `pe_tushare` 当前有值：**858**
- `pb_tushare` 当前有值：**1089**
- 当前已有历史数据，但尚未纳入 `app/` 新主线同步闭环

### 5. 当前总体判断
- **K 线主线已完成**
- **基本面同步已真实验证跑通**
- **估值同步逻辑存在，但仍停留在旧脚本，不在新主线内**
- **下一步最值钱的是把 `daily_basic` 正式迁入 `valuation_sync`**

---

## 二、当前建议执行顺序

## P1 - 扩大基本面同步（当前第一主线）

原因：
- 已确认 `fundamental_sync` 能真实写入
- 当前覆盖率仍极低
- 是页面可信度的最大短板

### 当前状态
已完成小批量验证：
- `limit=20`
- 成功更新 20 条
- 没有失败、没有频控

### 下一步建议
1. 再跑 `limit=100`
2. 再评估成功率 / 频控 / 字段有效率
3. 若稳定，再逐步扩大 nightly 自动批次

---

## P2 - 把 `daily_basic` 正式纳入 `valuation_sync` 主线

原因：
- `pe_tushare / pb_tushare` 已有历史值，但来源仍是旧脚本
- 目前不走：
  - `app/shared/settings.py`
  - `app/shared/db.py`
  - `TaskRunLogger`
  - 数据状态页统一状态
- 这条线是当前最值得工程化重构的一步

### 当前判断
老脚本 `update_pe_pb_tushare.py` 的核心思路可复用：
- 使用 `daily_basic(trade_date=...)`
- 获取全市场 PE/PB
- 按 code 回写 `stock_basic`

但实现方式需要迁移到：
- `app.data_ingestion.valuation_sync`

### 目标
将估值同步改造成新主线标准组件，具备：
1. 环境变量读取
2. MySQL 主线连接
3. task log 记录
4. cron 可调度
5. 数据状态页可展示最近同步状态

---

## P3 - 准备下一批 Tushare 接口接入

当前已明确可接入接口优先级：
1. `fina_indicator`
2. `daily_basic`
3. `cyq_perf`
4. `adj_factor`
5. `moneyflow`

### 当前建议
先不要同时铺太多接口，避免主线发散。

推荐节奏：
1. 先稳住 `fina_indicator`
2. 再正式接 `daily_basic`
3. 然后设计 `cyq_perf` 的数据表和同步器

---

## 三、对页面影响最大的字段优先级

### 第一优先级
1. `pe_tushare`
2. `pb_tushare`
3. `roe`
4. `revenue_yoy`

### 第二优先级
5. `roa`
6. `grossprofit_margin`
7. `netprofit_margin`

### 第三优先级
8. `profit_yoy`
9. `winner_rate`（未来 `cyq_perf`）
10. `weight_avg` / 筹码成本分位（未来 `cyq_perf`）

---

## 四、建议执行节奏

## 第一步（现在就执行）
继续扩大基本面同步：
- 从验证批次进入可持续批次
- 先 `100`，再 `300`

## 第二步
把 `daily_basic` 重构进 `valuation_sync`：
- 不继续依赖旧脚本裸跑
- 纳入统一 task log / cron / 数据状态展示

## 第三步
估值与基本面稳定后：
- 设计并接入 `cyq_perf`
- 优先服务选股解释与跟踪复盘

## 第四步
后续再推进：
- `adj_factor`
- `moneyflow`

---

## 五、当前不建议马上做的事

以下内容先别抢主线：

- 不先扩更多页面模块
- 不先做复杂前端图表
- 不先同时接太多 Tushare 接口
- 不先做深层财务三表建模

原因：

> **当前最值钱的不是继续扩功能，而是先把基本面和估值主链路工程化、稳定化。**

---

## 六、阶段性完成标准

### K 线阶段目标
- `daily_kline` 覆盖 **100%（股票口径）** ✅

### 估值阶段目标
- `pe_tushare` / `pb_tushare` 明显扩大覆盖
- 同步任务进入 `app/` 主线并自动化

### 基本面阶段目标
- `roe` / `revenue_yoy` / `roa` 覆盖率明显脱离个位数
- 至少先进入 **5%~10%+**，再继续冲更高

---

## 七、现在最值得直接开干的一步

### 推荐下一步
**正式推进 `daily_basic -> valuation_sync` 主线迁移。**

理由：
- `fina_indicator` 已经验证通了
- 日线主线也完成了
- 现在最该补上的工程短板，就是估值同步还没进入新体系

一句话：

> **历史日线已经补齐，现在主线正式切到“基本面 + 估值工程化补齐”。**
